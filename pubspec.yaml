name: shinobi_sprint
description: "A Game like Subway Surf but with Japanese style"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flame: ^1.19.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

